print('текст') - выводит на экран
input('введи: ') - запрашивает данные у пользователя
int('4') - преобразует строку в число
print(f'Строка {переменная}') - вставить переменную в строку

name = 'alisa' - присвоить переменной значение 
